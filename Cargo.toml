[package]
name = "app-pagebank"
edition = "2024"
description = "Page Bank"
version = "1.0.0"

[package.metadata.app]
account_id = "yeti"
schemas = "schemas/schema.graphql"
resources = "resources/*.rs"
static = { path = "web", root = "/", source = "source", spa = true, build = "npm install && npm run build" }

[dependencies]
tracing = "0.1"
