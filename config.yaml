name: "Page Bank"
app_id: "app-pagebank"
version: "1.0"
schemas:
  - schemas/schema.graphql

resources:
  - resources/*.rs

static_files:
  path: web
  spa: true
  build:
    sourceDir: source
    command: npm run build

env:
  PAGEBANK_ORIGIN_URL: "https://www.example.com"
