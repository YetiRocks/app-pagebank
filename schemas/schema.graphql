type PageCache @table(database: "app-pagebank") @store(evictAfter: 3600) @export {
  id: ID! @primaryKey
  url: String! @indexed
  pageContents: String
  contentType: String
  statusCode: Int
  cachedAt: String
}
